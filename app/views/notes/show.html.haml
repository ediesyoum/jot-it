#notes

  %h1= @note.title
  %p= @note.content

  %button=  link_to "All Notes", notes_path

  %button=  link_to "Edit", edit_note_path(@note)

  %button=  link_to "Delete", note_path(@note), method: :delete, data: {confirm: "Are you sure you want to delete?"}
